class Level {
  int difficulty;
  
  Level(){
    //difficulty = 1;
    //Ball.ballXSpeed = 1;
    //Ball.ballYSpeed = 1;
    //Platform.platformSpeed = 5;
  }
 
 void levelUp(){
   //difficulty++;
   //Ball.ballXSpeed += 0.5;
   //Ball.ballYSpeed += 0.5;
   //Platform.platformSpeed ++;
 }
 
 void gameOver(){
 }
}

//Start the game at Difficulty 1, speed of the ball liked to the difficulty, speed of the platform linked to the difficulty, reduce the size of the platform with the dicculty
